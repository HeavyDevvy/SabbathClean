# Fresh Start - Berry Events

Starting completely fresh with a clean codebase.

## Current Status
✓ **Clean Architecture**: Fresh project structure with minimal components
✓ **Basic Infrastructure**: Express server, React frontend, TypeScript setup
✓ **Development Environment**: Working development server on port 5000

## User Preferences
Preferred communication style: Simple, everyday language.
Project approach: Start from scratch with clean, minimal architecture.

## System Architecture

### Frontend Architecture
- React 18 with TypeScript
- TanStack Query for data fetching
- Tailwind CSS for styling
- Clean, minimal structure ready for development

### Backend Architecture
- Express.js with TypeScript
- RESTful API design
- In-memory storage for simplicity
- Clean separation of concerns

### Current Structure
```
├── client/
│   ├── src/
│   │   ├── App.tsx (main app component)
│   │   └── lib/queryClient.ts (data fetching setup)
├── server/
│   ├── routes.ts (API endpoints)
│   ├── storage.ts (data management)
│   └── index.ts (server entry point)
└── shared/
    └── schema.ts (type definitions)
```

## Next Steps
Ready to build new features based on user requirements.